{
  "name": "travlog-fx",
  "private": true,
  "type": "module",
  "description": "트래블로그 환율·여행 정산 PWA (정적, 빌드 없음)",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1"
  }
}
